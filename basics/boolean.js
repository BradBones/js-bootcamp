// === equality operator
// !== not equal operator
// < less than operator
// > greater than operator
// <= less than or equal to operator
// >= greater than or equal to operator

let temp = 180;
//let isFreezing = temp <= 32;
//console.log(isFreezing);

if (temp <= 32) {
    //console.log('It is freezing outside!');
}

if (temp >= 110) {
    //console.log('It is way too hot outside!');
}

// Challenge area

// Create an age variable set to your age
let age = 30;

// If 7 or under, print message about child pricing
// IF 65 or older, print message about senior discout

if (age <= 7) {
    console.log('Yay! You get a child discount');
} else if (age >= 65) {
    console.log('Congrats! You get a senior discount');
} else {
    console.log('Sorry, you have to pay full price.');
};



